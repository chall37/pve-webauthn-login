name: Check Upstream Updates

on:
  schedule:
    # Run daily at 9 AM UTC
    - cron: '0 9 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  check-updates:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check upstream repositories for updates
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Repositories to monitor
          REPOS=(
            "proxmox/pve-access-control"
            "proxmox/pve-manager"
            "proxmox/pve-http-server"
            "proxmox/proxmox-widget-toolkit"
          )

          # File to store last known commits
          STATE_FILE=".github/upstream-state.json"

          # Initialize state if it doesn't exist
          if [ ! -f "$STATE_FILE" ]; then
            echo '{}' > "$STATE_FILE"
          fi

          UPDATES=""
          NEW_STATE="{}"

          for repo in "${REPOS[@]}"; do
            echo "Checking $repo..."

            # Get latest commit on default branch
            LATEST=$(gh api "repos/$repo/commits?per_page=1" --jq '.[0].sha' 2>/dev/null || echo "")
            LATEST_DATE=$(gh api "repos/$repo/commits?per_page=1" --jq '.[0].commit.committer.date' 2>/dev/null || echo "")
            LATEST_MSG=$(gh api "repos/$repo/commits?per_page=1" --jq '.[0].commit.message' 2>/dev/null | head -1 || echo "")

            if [ -z "$LATEST" ]; then
              echo "  Failed to fetch $repo"
              continue
            fi

            # Get previously known commit
            PREVIOUS=$(cat "$STATE_FILE" | jq -r ".\"$repo\" // empty")

            echo "  Latest: ${LATEST:0:7}"
            echo "  Previous: ${PREVIOUS:0:7}"

            # Update state
            NEW_STATE=$(echo "$NEW_STATE" | jq --arg repo "$repo" --arg sha "$LATEST" '. + {($repo): $sha}')

            # Check if changed
            if [ -n "$PREVIOUS" ] && [ "$LATEST" != "$PREVIOUS" ]; then
              echo "  -> UPDATED!"
              UPDATES="${UPDATES}\n- **$repo**: [\`${PREVIOUS:0:7}\` → \`${LATEST:0:7}\`](https://github.com/$repo/compare/${PREVIOUS}...${LATEST})\n  - Latest: $LATEST_MSG\n  - Date: $LATEST_DATE"
            elif [ -z "$PREVIOUS" ]; then
              echo "  -> New tracking (no previous state)"
            else
              echo "  -> No change"
            fi
          done

          # Save new state to file
          echo "$NEW_STATE" > "$STATE_FILE"

          # Output results
          if [ -n "$UPDATES" ]; then
            echo "has_updates=true" >> $GITHUB_OUTPUT
            # Use a delimiter for multiline output
            EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
            echo "updates<<$EOF" >> $GITHUB_OUTPUT
            echo -e "$UPDATES" >> $GITHUB_OUTPUT
            echo "$EOF" >> $GITHUB_OUTPUT
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
          fi

      - name: Create branch and PR for state update
        if: steps.check.outputs.has_updates == 'true'
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          UPDATES: ${{ steps.check.outputs.updates }}
        run: |
          # Generate branch name with date
          BRANCH_NAME="upstream-update/$(date +%Y-%m-%d)"

          # Check if branch already exists (from earlier run today)
          if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
            echo "Branch $BRANCH_NAME already exists, updating it"
            git fetch origin "$BRANCH_NAME"
            git checkout "$BRANCH_NAME"
            git reset --hard origin/main
          else
            git checkout -b "$BRANCH_NAME"
          fi

          # Commit the state file update
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .github/upstream-state.json
          git commit -m "Update upstream state tracking" || {
            echo "No changes to commit"
            echo "pr_number=" >> $GITHUB_OUTPUT
            exit 0
          }

          # Push the branch
          git push -f origin "$BRANCH_NAME"

          # Write PR body to file to avoid shell escaping issues
          cat > /tmp/pr-body.md << 'ENDOFBODY'
## Upstream State Update

This PR updates the upstream state tracking file to acknowledge the detected changes.

Merge this PR after reviewing the associated issue and confirming compatibility.

---
*This PR was automatically created by the upstream monitoring workflow.*
ENDOFBODY

          # Check if PR already exists for this branch
          EXISTING_PR=$(gh pr list --head "$BRANCH_NAME" --state open --json number --jq '.[0].number // empty')

          if [ -n "$EXISTING_PR" ]; then
            echo "PR #$EXISTING_PR already exists"
            echo "pr_number=$EXISTING_PR" >> $GITHUB_OUTPUT
          else
            echo "Creating new PR"
            PR_URL=$(gh pr create \
              --title "Update upstream state tracking - $(date +%Y-%m-%d)" \
              --body-file /tmp/pr-body.md \
              --label "upstream-update")
            PR_NUMBER=$(echo "$PR_URL" | grep -oE '[0-9]+$')
            echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
          fi

      - name: Create or update issue
        if: steps.check.outputs.has_updates == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          UPDATES: ${{ steps.check.outputs.updates }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
        run: |
          # Check if there's already an open issue
          EXISTING=$(gh issue list --label "upstream-update" --state open --json number --jq '.[0].number // empty')

          # Write body to file to avoid shell escaping issues
          cat > /tmp/issue-body.md << 'ENDOFBODY'
## Upstream Proxmox Updates Detected

The following upstream repositories have new commits since last check:

ENDOFBODY

          echo "$UPDATES" >> /tmp/issue-body.md

          cat >> /tmp/issue-body.md << ENDOFBODY

## Associated PR

ENDOFBODY

          if [ -n "$PR_NUMBER" ]; then
            echo "Merge #$PR_NUMBER to acknowledge these changes and update the state tracking." >> /tmp/issue-body.md
          else
            echo "No PR was created (no state changes needed)." >> /tmp/issue-body.md
          fi

          cat >> /tmp/issue-body.md << 'ENDOFBODY'

## Action Required

1. Review the changes in the linked comparisons above
2. Check if any changes affect:
   - `PVE::AccessControl` (ticket/auth functions)
   - `PVE::HTTPServer::auth_handler`
   - `PVE::Service::pveproxy`
   - `PVE.window.LoginWindow` (ExtJS)
3. If changes affect compatibility:
   - Test the plugin against updated Proxmox
   - Update `compatibility.json` if a new PVE version needs to be added
   - Push any necessary changes to the PR branch
4. Merge the PR to acknowledge the upstream changes
5. Close this issue when validated

---
*This issue was automatically created by the upstream monitoring workflow.*
ENDOFBODY

          if [ -n "$EXISTING" ]; then
            echo "Updating existing issue #$EXISTING"
            gh issue comment "$EXISTING" --body-file /tmp/issue-body.md
          else
            echo "Creating new issue"
            gh issue create \
              --title "Upstream Proxmox updates detected - validation required" \
              --body-file /tmp/issue-body.md \
              --label "upstream-update"
          fi
